[build]
  base = "apps/dashboard-app-shell"
  command = "pnpm build"
  publish = "apps/dashboard-app-shell/dist"
  ignore = "git diff --quiet HEAD^ HEAD -- apps/dashboard-app-shell/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
